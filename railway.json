{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-interaction --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan storage:link --force && php-fpm -F"
  }
}
