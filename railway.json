{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-interaction --optimize-autoloader"
  },
  "deploy": {
    "port": 8080,
    "startCommand": "php artisan migrate --force && php artisan storage:link --force && php artisan serve --host=0.0.0.0 --port=${PORT:-3000}"
  }
}
