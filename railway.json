{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-interaction --optimize-autoloader"
  },
  "deploy": {
    "port": 8080,
    "startCommand": "mkdir -p storage/app/public/fonts && cp -n resources/fonts/* storage/app/public/fonts/ 2>/dev/null; php artisan storage:link --force 2>/dev/null; php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT:-3000}"
  }
}
